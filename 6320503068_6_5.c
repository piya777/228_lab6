#include<stdio.h>

    void main(){
        int w,h;
        scanf("%d %d",&w ,&h);
        int x[w][h]
        for( i=0 ; i<h ; i++ ){
            for( j=0 ; j<w ; j++ ){
                scanf("%d",&x[i][j]);
            }
        }
    }
